node_modules/
.next/
out/
build/
.vercel
*.tsbuildinfo
next-env.d.ts

# Raw compiler output. The published artifacts (contract/artifacts and public/zk)
# are committed instead, so a Vercel build never needs the Compact toolchain.
contract/build/

# Local review scratch (screenshots, browser drivers).
.screens/
.capture.mjs
.drive*.mjs
.probe.mjs
.verify.mjs
.srv.log

# Agent working directory: private mandate rules and secrets live here.
.mandate/
*.private-state.json

.env
.env.local
.env*.local
.DS_Store
